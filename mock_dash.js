const axios = require('axios');
require('dotenv').config();

async function check() {
    const coId = '69caf340162fc71dc07307d1';
    // I can't call it directly without token.
    // I'll just write a script that connects to DB and runs the EXACT SAME aggregated queries.
}
